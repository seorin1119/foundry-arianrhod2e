{{!-- Sheet Tabs --}}
<nav class="sheet-tabs tabs" data-group="primary">
  {{#each tabs}}
  <a class="item {{cssClass}}" data-tab="{{id}}" data-group="primary" data-action="tab">
    {{#if icon}}<i class="{{icon}}"></i> {{/if}}{{localize label}}
  </a>
  {{/each}}
</nav>
